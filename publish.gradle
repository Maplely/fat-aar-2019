apply plugin: 'maven'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri(project.REPOSITORY))
            pom.project {
                groupId project.GROUPID
                artifactId project.ARTIFACTID
                version project.VERSION
            }
        }
    }
}

