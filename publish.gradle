apply plugin: 'maven'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri("/Users/bkjk/Downloads"))
            pom.project {
                groupId "com.top.plugin"
                artifactId "fat-aar"
                version "1.0.0-SNAPSHOT"
            }
        }
    }
}

